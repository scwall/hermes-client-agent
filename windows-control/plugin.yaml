name: windows-control
version: 1.0.0
description: Remote Windows PC control via HTTP REST API. Execute commands, control mouse/keyboard, manage windows, capture screenshots.
provides_hooks:
  - on_session_start
